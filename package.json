{
  "dependencies": {
    "bootstrap": "^5.3.6",
    "typescript": "^5.8.3"
  },
  "scripts": {
    "build": "tsc && cp index.html dist/ && cp styles.css dist/ && cp logo.png dist/ && cp bg.png dist/ && cp Placeholder.png dist/"
  }
}
